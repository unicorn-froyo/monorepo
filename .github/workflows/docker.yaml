name: Docker Build

on:
  pull_request:
    branches:
      - main
    paths:
      - ".github/**"

jobs:
  docker_build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Docker Build
        run: docker build .github -f .github/Dockerfile
