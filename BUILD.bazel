load("@com_github_bazelbuild_buildtools//buildifier:def.bzl", "buildifier")
load("//tools/lint/bazel:index.bzl", "bazel_lint_test")

buildifier(
    name = "buildifier",
)

bazel_lint_test(
    name = "workspace_bazel_lint_test",
    data = [
        "*.bazel",
        "WORKSPACE",
    ],
)
